import { RankFormatPipe } from './rank-format.pipe';

describe('RankFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RankFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
