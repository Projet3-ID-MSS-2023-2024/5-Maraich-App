import { AdressFormatPipe } from './adress-format.pipe';

describe('AdressFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AdressFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
